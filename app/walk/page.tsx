"use client";

// Placeholder — capture UI lands in Block 2 (camera, STT, LOG EVENT).
export default function WalkPage() {
  return (
    <div className="p-4 text-xs uppercase tracking-widest text-nominal">
      &gt; WALK MODULE — PENDING BLOCK 2
    </div>
  );
}
